import * as React from 'react';
import ScopedCssBaseline from '@temp-mui/material/ScopedCssBaseline';

<ScopedCssBaseline
  sx={(theme) => ({
    backgroundColor: theme.palette.background.paper,
  })}
/>;
